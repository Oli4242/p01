class KeyGroup {
  constructor(game, keycodes) {
    this.game = game
    this.keycodes = Array.isArray(keycodes) ? keycodes : [keycodes]
    this.keys = []

    if (!Array.isArray(this.keycodes))
      this.keycodes = this.keycodes

    keycodes.
    for (let keycode in keycodes) {

    }
  }


}
